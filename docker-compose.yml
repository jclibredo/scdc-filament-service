services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: filament_app
    restart: unless-stopped
    tty: true
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ".:/var/www"
      - "./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini"
    networks:
      - sail

  webserver:
    image: "nginx:alpine"
    container_name: filament_webserver
    restart: unless-stopped
    ports:
      # - '8081:80'
      - "0.0.0.0:8081:80"
    volumes:
      - ".:/var/www"
      - "./docker/nginx:/etc/nginx/conf.d/"
    networks:
      - sail

  # mysql:
  #   image: "mysql:8.0"
  #   container_name: filament_db
  #   restart: unless-stopped
  #   ports:
  #     - "${FORWARD_DB_PORT:-3307}:3306"
  #   environment:
  #     MYSQL_ROOT_PASSWORD: "${DB_PASSWORD:-1234JCuer!}"
  #     MYSQL_ROOT_HOST: "%"
  #     MYSQL_DATABASE: "${DB_DATABASE:-scdc_db}"
  #     MYSQL_USER: "${DB_USERNAME:-root}"
  #     MYSQL_PASSWORD: "${DB_PASSWORD:-1234JCuer!}"
  #     MYSQL_ALLOW_EMPTY_PASSWORD: 1
  #   volumes:
  #     - "sail-mysql:/var/lib/mysql"
  #   networks:
  #     - sail
  #   healthcheck:
  #     test:
  #       - CMD
  #       - mysqladmin
  #       - ping
  #       - "-p${DB_PASSWORD:-1234JCuer!}"
  #     retries: 3
  #     timeout: 5s

networks:
  sail:
    driver: bridge

volumes:
  sail-mysql:
    driver: local
